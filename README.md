# Packer templates to create Vagrant Box from scratch

This repository contains the required scripts for Packer to build Linux based Virtualbox compatible Vagrant boxes starting from a net-install iso.